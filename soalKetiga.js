// celcius = 0 - 100
// fahrenheit = 32 - 212
// reamur = 0 - 80
// kelvin = 273 - 373
function getRandom(min, max) {
    return Math.floor(Math.random() * max - min + min);
}

let celcius = getRandom(0, 100);
let fahrenheit = getRandom(32, 212);
let reamur = getRandom(0, 80);
let kelvin = getRandom(273, 373);

function konveriSuhu(suhuAwal, suhuAkhir) {
    if 
}